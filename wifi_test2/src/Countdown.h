#include "LocalDatabase.h"

class Countdown
{
private:
    /* data */
public:
    Countdown(/* args */);
    ~Countdown();

    void displayCountdown(String time, int hour, int min, int sec, timetableEntry *timetable);
};





